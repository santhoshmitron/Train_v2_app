
clustering {
  ip = "127.0.0.1"
  cluster.name = finlr
  masterport = "2558"
  masternode = "127.0.0.1"
}

squbs {
  actorsystem-name = ${clustering.cluster.name}
}

default-listener{
  bind-port = 8081
}

kamon.util.filters {
  "akka.tracked-actor" {
    includes = [ "**" ]
  }
}


akka {
  loggers = ["akka.event.slf4j.Slf4jLogger"]
  loglevel = "INFO"

  remote {
    log-remote-lifecycle-events = off
    netty.tcp {
      hostname = ${clustering.ip}
      port = 2558
    }
  }

  actor {
    provider = "akka.cluster.ClusterActorRefProvider"
  }

  cluster {
    seed-nodes = [
      "akka.tcp://"${clustering.cluster.name}"@"${clustering.masternode}":"${clustering.masterport}
      //"akka.tcp://"${clustering.name}"@"${clustering.ip}":"${clustering.masterport}
      //"akka.tcp://"${clustering.cluster.name}"@"${clustering.ip}":2552"
    ]
    log-info = on
    auto-down-unreachable-after = 10s
    extensions = ["akka.cluster.ddata.DistributedData"]
  }

  tracing {
    host = "localhost"
#    port = 9410 # default
    # port = 9011 # default
  }
  actor.warn-about-java-serializer-usage = false

  serializers {
    java = "akka.serialization.JavaSerializer"
  }

  // Persistance Configuration
  persistence.journal.plugin = "akka.persistence.journal.leveldb"
  persistence.snapshot-store.plugin = "akka.persistence.snapshot-store.local"

  persistence.journal.leveldb.dir = "target/finlr/journal"
  persistence.snapshot-store.local.dir = "target/finlr/snapshots"
  // DO NOT USE THIS IN PRODUCTION
  persistence.journal.leveldb.native = false

}


slick-ignite {
  profile = "slick.jdbc.MySQLProfile$"
  db {
    connectionPool = disabled
    dataSourceClass = "slick.jdbc.DatabaseUrlDataSource"
    properties = {
      driver = "org.apache.ignite.IgniteJdbcThinDriver"
      url = "jdbc:ignite:thin://localhost"
    }
  }
}


rtrs
  {
    kafka {
      consumer_finl {
        server="localhost:9092",
        consumerGroup="finlGroup",
        topic="topic2",
        offset_reset="latest",
        clientId="clientFinl"
      }
      consumer_finr{
        server="localhost:9092",
        consumerGroup="finrGroup",
        topic="topic1"
        offset_reset="latest",
        clientId="clientFinr"
      }
      producer_finlr{
        server="localhost:9092",
        consumerGroup="finlrGroup"
        topic="topic6"
      }
      log_topic{
        topic="kafka.fe.log"
      }
    }
    mandatoryParameters{
      verify=true
    }
    wallet{
      host = "10.144.108.106:9001"
    }
    data{
      origin = "DNDBRPSL"
    }
    ignite{
      ip="localhost",
      maxTotal = 1000,
      minIdle = 100,
      poolName="finlrIgnitePool",
      connectionPooling="false",
      retry=1
    }
    cluster{
      duration = 60
      shardId = 2
    }
    pel{
      enable = "false"
    }
    privileged{
      enable = "false"
    }
    pel{
      enable = "false"
    }
    jpb{
      enable = "false"
    }
  }

